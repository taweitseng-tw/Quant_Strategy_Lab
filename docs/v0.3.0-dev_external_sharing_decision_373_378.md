# v0.3.0-dev External Sharing Decision - Tasks 373-378

> Decision document. No upload, push, or remote configuration was executed.
> Generated: 2026-06-11

## Current Artifact

| Property | Value |
|---|---|
| Zip path | `release_artifacts/QuantStrategyLab-v0.3.0-dev-windows-onedir.zip` |
| Zip size | 128,770,261 bytes |
| Zip size | 122.8 MiB |
| Gitignored | Yes, via `release_artifacts/` |
| Tag | `v0.3.0-dev`, local only |
| Remote | No remote configured |

## Options

| Option | Summary | Fit |
|---|---|---|
| Keep zip local only | Leave the zip on this machine. | Safest, but not shareable. |
| Manually send zip to evaluator | Send through a direct file transfer method chosen by the user. | Good for one trusted evaluator. |
| Upload zip to cloud storage | Upload to user-controlled cloud storage and share a private link. | Best immediate sharing path for multiple evaluators. |
| Configure remote and push tag later | Add a remote, push tag, and later attach artifacts through a release flow. | Better long-term, not needed for immediate sharing. |

## Recommendation

Use user-controlled cloud storage only when the user is ready to share the
developer pre-release zip.

Do not upload from this task. Do not push the tag from this task.

Rationale:

1. The zip is already created and ignored by git.
2. There is no configured git remote.
3. A cloud link is lower friction than asking evaluators to build from source.
4. The artifact is a developer pre-release, not a signed production installer.

## Suggested Manual Steps

1. Confirm the zip exists:

```powershell
Get-Item .\release_artifacts\QuantStrategyLab-v0.3.0-dev-windows-onedir.zip
```

2. Upload the zip manually using the user's preferred cloud storage UI.

3. Share the link only with intended evaluators.

4. Include this warning with the link:

```text
This is a Windows developer pre-release for research and backtesting only.
It is not code-signed, not an installer, not investment advice, and not for live trading.
```

## Verification Commands

```powershell
git status --short
git check-ignore .\release_artifacts\QuantStrategyLab-v0.3.0-dev-windows-onedir.zip
git diff --check
git remote -v
```

Expected:

```text
Zip remains ignored.
No upload was executed.
No push was executed.
No remote is configured.
```

## Remaining Risks

1. The zip is not code-signed.
2. Cloud sharing has link-permission risk.
3. The package is Windows-only and large.
4. No public release process exists yet.

## Next Step

No additional repository task is required unless the user wants Codex to help
prepare a share message, configure a remote, or design a proper GitHub Release
workflow later.
