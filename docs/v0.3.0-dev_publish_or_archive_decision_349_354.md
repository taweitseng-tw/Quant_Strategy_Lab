# v0.3.0-dev Publish or Archive Decision - Tasks 349-354

> Decision document. No push was executed.
> Generated: 2026-06-10

## Current State

| Property | Value |
|---|---|
| Tag name | `v0.3.0-dev` |
| Tag object hash | `ed34d2b5373c3fb8417839b9f06b67e2f706cebe` |
| Target commit hash | `bd94e90bd82839f8e47d21bbda5dc80cc04c8003` |
| Target commit message | `docs: add v0.3.0-dev release notes` |
| Remote configured | No remote is currently configured. |
| Current status target | Clean working tree. |

## Decision

Keep `v0.3.0-dev` as a local release-candidate checkpoint for now.

Do not push in this task.

## Rationale

1. The tag is correctly created and points to the release notes commit.
2. The working tree is clean after the release notes commit.
3. No remote is configured, so `git push origin v0.3.0-dev` is not currently
   actionable.
4. The tag is still a developer pre-release, not a public production release.
5. A later publish task should decide where to push and whether to attach build
   artifacts or release notes outside git.

## Push Command for Later

Only after a remote is configured and the user approves publishing:

```powershell
git push origin v0.3.0-dev
```

If no `origin` remote exists, configure or choose the intended remote first.

## Verification Commands

```powershell
git status --short
git show --no-patch --format=fuller v0.3.0-dev
git rev-parse v0.3.0-dev
git rev-list -n 1 v0.3.0-dev
git remote -v
git diff --check
```

Expected:

```text
Status is clean.
Tag object is ed34d2b5373c3fb8417839b9f06b67e2f706cebe.
Tag target commit is bd94e90bd82839f8e47d21bbda5dc80cc04c8003.
No push has been executed.
```

## Remaining Risks

1. Local tags are not shared unless pushed or archived externally.
2. No remote publishing destination is configured.
3. No release artifact upload flow exists yet.
4. The package is still a developer pre-release build.

## Next Recommended Task

Tasks 355-360 - Release Artifact Archive Plan

Decide how to store or share the packaged output without committing `dist/`,
`build/`, `.exe`, or generated `.spec` files to source control.
